<UserControl x:Class="NetErp.Billing.PriceList.Views.PriceListMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:h="clr-namespace:NetErp.Helpers"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:services="clr-namespace:NetErp.Helpers.Services"
             xmlns:priceListHelpers="clr-namespace:NetErp.Billing.PriceList.PriceListHelpers"
             xmlns:busyIndicator="https://github.com/moh3ngolshani/BusyIndicator"
             xmlns:uc="clr-namespace:NetErp.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:Background="White">
    <UserControl.Resources>
        <services:OperationStatusToColorConverter x:Key="StatusToColorConverter"/>
        <h:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter"/>
        <priceListHelpers:IvaValueConverter x:Key="IvaValueConverter"/>
    </UserControl.Resources>
    
    <Border Background="White" BorderBrush="#D9DDE3" BorderThickness="1"
            CornerRadius="8" Margin="5">
    <busyIndicator:BusyMask x:Name="BusyIndicator"
                    IsBusy="{Binding MainIsBusy}"
                    IndicatorType="BouncingDot"
                    BusyContent="Por favor espere..."
                    BusyContentMargin="0,20,0,0">
        <Grid>

            <uc:DependencyStateView
                Visibility="{Binding HasUnmetDependencies, Converter={dxmvvm:BooleanToVisibilityConverter}}"
                Title="Configuración requerida para Listas de precios"
                Message="Antes de registrar listas de precios, debe configurar los siguientes elementos:"
                Dependencies="{Binding Dependencies}"/>

            <uc:EmptyStateView Visibility="{Binding CanShowEmptyState, Converter={dxmvvm:BooleanToVisibilityConverter}}"
                               Title="No hay listas de precios registradas"
                               Message="Cree una nueva lista de precios para comenzar"
                               ContextInfo="Las listas de precios permiten manejar precios predefinidos para diferentes tipos de clientes, por ejemplo: precio al público o precio mayorista."
                               Command="{Binding CreatePriceListCommand}"
                               ButtonText="Crear lista de precios"/>

            <Grid Visibility="{Binding HasRecords, Converter={dxmvvm:BooleanToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Búsqueda -->
                <dxe:ButtonEdit x:Name="FilterSearch"
                                Grid.Row="0"
                                Text="{Binding FilterSearch, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                NullText="Escribe por lo menos 2 caracteres para iniciar la búsqueda"
                                ToolTip="Escribe aquí para buscar"
                                MinHeight="25"
                                CharacterCasing="Upper"
                                BorderBrush="#C8CED6"
                                AllowDefaultButton="False"
                                h:FocusBehavior.IsFocused="True"
                                Margin="10,10,10,0">
                    <dxe:ButtonEdit.Buttons>
                        <dxe:ButtonInfo IsLeft="True" GlyphKind="Search" Margin="4,0,2,0"/>
                    </dxe:ButtonEdit.Buttons>
                </dxe:ButtonEdit>

                <!-- Filtros -->
                <Grid Grid.Row="1" Margin="10,6,10,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Vertical" Grid.Column="0" Grid.Row="0" Margin="0,0,4,6">
                        <TextBlock Text="Catálogo de productos" Margin="0,0,0,3"/>
                        <dxe:ComboBoxEdit ItemsSource="{Binding Catalogs, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedCatalog, UpdateSourceTrigger=PropertyChanged}"
                                          IsTextEditable="False" DisplayMember="Name" Padding="2" BorderBrush="#C8CED6"/>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Grid.Column="0" Grid.Row="1" Margin="0,0,4,0">
                        <TextBlock Text="Lista" Margin="0,0,0,3"/>
                        <dxe:ComboBoxEdit ItemsSource="{Binding PriceLists, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedPriceList, UpdateSourceTrigger=PropertyChanged}"
                                          IsTextEditable="False" DisplayMember="FullName" ApplyItemTemplateToSelectedItem="True" Padding="2" BorderBrush="#C8CED6">
                            <dxe:ComboBoxEdit.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Foreground" Value="Red"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </dxe:ComboBoxEdit.ItemTemplate>
                        </dxe:ComboBoxEdit>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Grid.Column="1" Grid.Row="1" Margin="4,0,4,0">
                        <TextBlock Text="Tipo de producto" Margin="0,0,0,3"/>
                        <dxe:ComboBoxEdit ItemsSource="{Binding ItemTypes, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedItemType, UpdateSourceTrigger=PropertyChanged}"
                                          IsTextEditable="False" DisplayMember="Name" Padding="2" BorderBrush="#C8CED6"
                                          AllowNullInput="True" NullValueButtonPlacement="EditBox" NullText="Todos los tipos"/>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Grid.Column="2" Grid.Row="1" Margin="4,0,4,0" Visibility="{Binding CanShowItemCategories, UpdateSourceTrigger=PropertyChanged, Converter={dx:BooleanToVisibilityConverter}}">
                        <TextBlock Text="Línea/Categoría" Margin="0,0,0,3"/>
                        <dxe:ComboBoxEdit ItemsSource="{Binding ItemCategories, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedItemCategory, UpdateSourceTrigger=PropertyChanged}"
                                          IsTextEditable="False" DisplayMember="Name" Padding="2" BorderBrush="#C8CED6"
                                          AllowNullInput="True" NullValueButtonPlacement="EditBox" NullText="Todas las categorías"/>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Grid.Column="3" Grid.Row="1" Margin="4,0,0,0" Visibility="{Binding CanShowItemSubCategories, UpdateSourceTrigger=PropertyChanged, Converter={dx:BooleanToVisibilityConverter}}">
                        <TextBlock Text="Sublínea/Subcategoría" Margin="0,0,0,3"/>
                        <dxe:ComboBoxEdit ItemsSource="{Binding ItemSubCategories, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedItemSubCategory, UpdateSourceTrigger=PropertyChanged}"
                                          IsTextEditable="False" DisplayMember="Name" Padding="2" BorderBrush="#C8CED6"
                                          AllowNullInput="True" NullValueButtonPlacement="EditBox" NullText="Todas las subcategorías"/>
                    </StackPanel>
                </Grid>

                <!-- Banner inactiva -->
                <StackPanel Grid.Row="2" Margin="10,6,10,0" Visibility="{Binding SelectedPriceListIsNotActive, UpdateSourceTrigger=PropertyChanged, Converter={dxmvvm:BooleanToVisibilityConverter}}" Background="Red">
                    <TextBlock Text="ESTA LISTA DE PRECIOS NO ESTÁ ACTIVA" HorizontalAlignment="Center" Foreground="White" FontSize="10" FontWeight="SemiBold"/>
                </StackPanel>

                <!-- Grid -->
                <busyIndicator:BusyMask
                    IsBusy="{Binding IsBusy}"
                    IndicatorType="BouncingDot"
                    BusyContent="Por favor espere..."
                    BusyContentMargin="0,20,0,0"
                    Grid.Row="3" Margin="10,6,10,0">

                    <dxg:GridControl ItemsSource="{Binding PriceListItems, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             SelectedItem="{Binding SelectedPriceListItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <dxg:GridControl.View>
                            <dxg:TableView
                                    AllowCascadeUpdate="False" 
                                    EnableSelectedRowAppearance="True"
                                    ShowGroupPanel="False" AlternateRowBackground="Beige"
                                    AllowColumnMoving="False" ColumnFilterPopupMode="Disabled"
                                    AllowSorting="False" AllowColumnFiltering="False">
                                <dxg:TableView.RowStyle>
                                    <Style TargetType="dxg:RowControl">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Row.Price}" Value="0">
                                                <Setter Property="TextBlock.Foreground" Value="Red"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                            </DataTrigger>
                                            <!-- Estilos para estados de operación -->
                                            <DataTrigger Binding="{Binding Row.Status}" Value="Pending">
                                                <Setter Property="Background" Value="#FFEED0"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Row.Status}" Value="Saved">
                                                <Setter Property="Background" Value="#E0FFE0"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Row.Status}" Value="Failed">
                                                <Setter Property="Background" Value="#FFE0E0"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </dxg:TableView.RowStyle>
                            </dxg:TableView>
                        </dxg:GridControl.View>

                        <dxg:GridColumn Header="Código" Width="65" HorizontalHeaderContentAlignment="Center" Binding="{Binding Item.Code, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxg:GridColumn Header="Nombre del producto" Width="*" Binding="{Binding Item.Name, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxg:GridColumn Header="Referencia" HorizontalHeaderContentAlignment="Center" Binding="{Binding Item.Reference, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxg:GridColumn Header="Stock" Width="50" HorizontalHeaderContentAlignment="Center" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxg:GridColumn Header="UND" Binding="{Binding Item.MeasurementUnit.Abbreviation, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                        <dxg:GridColumn Header="Costo" Width="65" HorizontalHeaderContentAlignment="Center" Binding="{Binding Cost, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxg:GridColumn Header="IVA" Width="Auto" HorizontalHeaderContentAlignment="Center" Binding="{Binding IVA, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource IvaValueConverter}}"/>
                        <dxg:GridColumn Header="%Util" Width="60" HorizontalHeaderContentAlignment="Center" Binding="{Binding ProfitMargin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ReadOnly="{Binding IsGridReadOnly, UpdateSourceTrigger=PropertyChanged}">
                            <dxg:GridColumn.EditSettings>
                                <dxe:TextEditSettings MaskType="Numeric" MaskCulture="en-US" MaskUseAsDisplayFormat="True" AllowSpinOnMouseWheel="False"
                                          Mask="{Binding MaskN2, UpdateSourceTrigger=PropertyChanged}"/>
                            </dxg:GridColumn.EditSettings>
                        </dxg:GridColumn>
                        <dxg:GridColumn Header="Utilidad" Width="80" HorizontalHeaderContentAlignment="Center" Binding="{Binding Profit, UpdateSourceTrigger=PropertyChanged}">
                            <dxg:GridColumn.EditSettings>
                                <dxe:TextEditSettings MaskType="Numeric" MaskCulture="en-US" MaskUseAsDisplayFormat="True" AllowSpinOnMouseWheel="False"
                                          Mask="{Binding MaskN2, UpdateSourceTrigger=PropertyChanged}"/>
                            </dxg:GridColumn.EditSettings>
                        </dxg:GridColumn>
                        <dxg:GridColumn Header="Precio de venta" HorizontalHeaderContentAlignment="Center" Binding="{Binding Price, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ReadOnly="{Binding IsGridReadOnly, UpdateSourceTrigger=PropertyChanged}">
                            <dxg:GridColumn.EditSettings>
                                <dxe:TextEditSettings MaskType="Numeric" MaskCulture="en-US" MaskUseAsDisplayFormat="True" AllowSpinOnMouseWheel="False"
                                          Mask="{Binding MaskN2, UpdateSourceTrigger=PropertyChanged}"/>
                            </dxg:GridColumn.EditSettings>
                        </dxg:GridColumn>
                        <dxg:GridColumn Header="Precio minimo" HorizontalHeaderContentAlignment="Center" Binding="{Binding MinimumPrice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ReadOnly="{Binding IsGridReadOnly, UpdateSourceTrigger=PropertyChanged}">
                            <dxg:GridColumn.EditSettings>
                                <dxe:TextEditSettings MaskType="Numeric" MaskCulture="en-US" MaskUseAsDisplayFormat="True" AllowSpinOnMouseWheel="False"
                                          Mask="{Binding MaskN2, UpdateSourceTrigger=PropertyChanged}"/>
                            </dxg:GridColumn.EditSettings>
                        </dxg:GridColumn>
                        <dxg:GridColumn Header="%Dcto" Width="60" HorizontalHeaderContentAlignment="Center" Binding="{Binding DiscountMargin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ReadOnly="{Binding IsGridReadOnly, UpdateSourceTrigger=PropertyChanged}">
                            <dxg:GridColumn.EditSettings>
                                <dxe:TextEditSettings MaskType="Numeric" MaskCulture="en-US" MaskUseAsDisplayFormat="True" AllowSpinOnMouseWheel="False"
                                          Mask="{Binding MaskN5, UpdateSourceTrigger=PropertyChanged}"/>
                            </dxg:GridColumn.EditSettings>
                        </dxg:GridColumn>
                    </dxg:GridControl>
                </busyIndicator:BusyMask>

                <!-- Info impuesto -->
                <StackPanel Grid.Row="4" Margin="10,6,10,0" Visibility="{Binding IsPriceList, UpdateSourceTrigger=PropertyChanged, Converter={dxmvvm:BooleanToVisibilityConverter}}">
                    <ContentControl>
                        <ContentControl.Style>
                            <Style TargetType="{x:Type ContentControl}">
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding Path=SelectedPriceList.IsTaxable}" Value="true"/>
                                            <Condition Binding="{Binding Path=SelectedPriceList.PriceListIncludeTax}" Value="true"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Image Source="{dx:DXImage 'Images/Function Library/Information_16x16.png'}" Width="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="Esta lista de precios discrimina impuesto, el precio de venta incluye impuesto" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding Path=SelectedPriceList.IsTaxable}" Value="true"/>
                                            <Condition Binding="{Binding Path=SelectedPriceList.PriceListIncludeTax}" Value="false"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Image Source="{dx:DXImage 'Images/Function Library/Information_16x16.png'}" Width="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="Esta lista de precios discrimina impuesto, el precio de venta NO incluye impuesto" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </MultiDataTrigger>
                                    <DataTrigger Binding="{Binding Path=SelectedPriceList.IsTaxable}" Value="false">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Image Source="{dx:DXImage 'Images/Function Library/Information_16x16.png'}" Width="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="Esta lista de precios no discrimina impuesto" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>
                </StackPanel>

                <!-- Info costo -->
                <StackPanel Grid.Row="5" Margin="10,6,10,0" Visibility="{Binding IsPriceList, UpdateSourceTrigger=PropertyChanged, Converter={dxmvvm:BooleanToVisibilityConverter}}">
                    <ContentControl>
                        <ContentControl.Style>
                            <Style TargetType="{x:Type ContentControl}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=CostByStorageInformation}" Value="true">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Image Source="{dx:DXImage 'Images/Function Library/Information_16x16.png'}" Width="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="El costo de referencia para esta lista de precios es tomado de la bodega: " VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding SelectedPriceList.Storage.Name, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" FontWeight="Bold"/>
                                                    </StackPanel>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Path=CostByStorageInformation}" Value="false">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Image Source="{dx:DXImage 'Images/Function Library/Information_16x16.png'}" Width="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                        <TextBlock VerticalAlignment="Center">
                                                            <Run Text="El costo de referencia para esta lista de precios está basado en el"/>
                                                            <Run Text="costo promedio" FontWeight="Bold"/>
                                                            <Run Text="de las bodegas con existencias."/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>
                </StackPanel>

                <!-- Stock por bodega -->
                <Border Grid.Row="6" Margin="10,6,10,0" BorderThickness="1" BorderBrush="#DDDDDD" Visibility="{Binding ShowInventoryQuantity, UpdateSourceTrigger=PropertyChanged, Converter={dx:BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="STOCK POR BODEGA" HorizontalAlignment="Center" FontSize="14" FontWeight="SemiBold" Margin="0,4,0,0"/>
                        <ItemsControl ItemsSource="{Binding SelectedPriceListItem.Item.Stocks, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border MinWidth="120" Margin="2,4,2,4">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" MinHeight="40"/>
                                                <RowDefinition Height="Auto" MinHeight="30"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Grid.Row="0"
                                                       Text="{Binding Storage.Name}"
                                                       FontWeight="SemiBold"
                                                       TextAlignment="Center"
                                                       TextWrapping="Wrap"
                                                       Background="Transparent"
                                                       Padding="5"
                                                       VerticalAlignment="Center"/>
                                            <dxe:TextEdit Grid.Row="1"
                                                          Text="{Binding Quantity, Mode=OneWay}"
                                                          MaskType="Numeric"
                                                          MaskUseAsDisplayFormat="True"
                                                          Mask="N2"
                                                          HorizontalContentAlignment="Center"
                                                          IsReadOnly="True"
                                                          Padding="5"
                                                          BorderThickness="0"
                                                          FontWeight="Bold"
                                                          FontFamily="Cambria"
                                                          FontSize="30"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Paginación -->
                <uc:GridPaging Grid.Row="7"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Center"
                               Margin="10,6,10,0"
                               ChangedIndexCommand="{Binding PaginationCommand}"
                               PageIndex="{Binding PageIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                               PageSize="{Binding PageSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                               TotalCount="{Binding TotalCount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                               ResponseTime="{Binding ResponseTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Botones de acción -->
                <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,10">
                    <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                            Command="{Binding CreatePriceListCommand}">
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Actions/New_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.7"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="Nueva lista" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                            IsEnabled="{Binding CanCreatePromotion, UpdateSourceTrigger=PropertyChanged}"
                            Command="{Binding CreatePromotionCommand}">
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Actions/New_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.7"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="Nueva promoción" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                            Command="{Binding DeletePriceListCommand}">
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Actions/DeleteList_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.7"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="Eliminar" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                            Command="{Binding ConfigurationCommand}">
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Setup/Properties_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.7"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="Configuración" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

            </Grid>
        </Grid>
    </busyIndicator:BusyMask>
    </Border>
</UserControl>
