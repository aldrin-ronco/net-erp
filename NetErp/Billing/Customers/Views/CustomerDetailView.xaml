<UserControl x:Class="NetErp.Billing.Customers.Views.CustomerDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetErp.Billing.Customers.Views"
             xmlns:h="clr-namespace:NetErp.Helpers"
             xmlns:busyIndicator="https://github.com/moh3ngolshani/BusyIndicator"
             xmlns:cal="http://caliburnmicro.com"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:dxr="http://schemas.devexpress.com/winfx/2008/xaml/ribbon"
             xmlns:dxc="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:config="clr-namespace:Common.Config;assembly=Common" 
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="800" d:Background="White">
    <UserControl.Resources>
        <h:BindingProxy x:Key="DataContextProxy" Data="{Binding}"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <h:SimpleBooleanConverter x:Key="SimpleBooleanConverter" True="False" False="True"/>
        <h:EnumBooleanConverter x:Key="EnumBooleanConverter"/>
        <SolidColorBrush Color="Pink" Opacity="0.4" x:Key="ValidationBrush"/>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip">
                        <Setter.Value>
                            <ToolTip DataContext="{Binding RelativeSource={RelativeSource Self}, Path=PlacementTarget}">
                                <ItemsControl ItemsSource="{Binding Path=(Validation.Errors)}" DisplayMemberPath="ErrorContent" />
                            </ToolTip>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Background" Value="{StaticResource ValidationBrush}"/>
                    <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
                </Trigger>
            </Style.Triggers>
        </Style>       
    </UserControl.Resources>
    <busyIndicator:BusyMask x:Name="BusyIndicator"
                            IsBusy="{Binding IsBusy}"
                            IndicatorType="BouncingDot"
                            BusyContent="Please wait..."
                            BusyContentMargin="0,20,0,0"
                            IsBusyAtStartup="False"
                            Grid.Row="3">
        <Grid h:EnterKeyTraversal.IsEnabled="True">

            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="0"/>
            </Grid.ColumnDefinitions>

            <!--Toolbar-->
            <dxr:RibbonControl Grid.Column="0" Grid.ColumnSpan="3" IsSimplified="False" 
                           AllowSimplifiedRibbon="True" ShowApplicationButton="False" ToolbarShowMode="Hide" 
                           RibbonStyle="Office2010" ToolbarShowCustomizationButton="False" RibbonTitleBarVisibility="Collapsed" VerticalAlignment="Center">

                <dxr:RibbonPage Caption="General">

                    <dxr:RibbonPageGroup Caption="Acciones">
                        <dxb:BarButtonItem x:Name="btnBack" 
                                       Glyph="{dx:DXImage 'Images/Navigation/Backward_16x16.png'}"
                                       LargeGlyph="{dx:DXImage 'Images/Navigation/Backward_32x32.png'}" 
                                       Content="Regresar"
                                       RibbonStyle="Large"
                                       Hint="Regresa a la lista de proveedores"
                                       Command="{Binding Path=GoBackCommand}"/>
                        <dxb:BarItemSeparator/>

                        <dxb:BarButtonItem Content="Guardar" 
                                           Glyph="{dx:DXImage 'Images/Save/Save_16x16.png'}" 
                                           LargeGlyph="{dx:DXImage 'Images/Save/Save_32x32.png'}"                                             
                                           Description="Creates a new document."
                                           Hint="Guarda los cambios realizados"    
                                           IsEnabled="{Binding Path=CanSave, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                           RibbonStyle="Large">
                            <dxmvvm:Interaction.Behaviors>
                                <dxmvvm:EventToCommand Command="{Binding Path=SaveCommand}" EventName="ItemClick"/>
                            </dxmvvm:Interaction.Behaviors>
                        </dxb:BarButtonItem>
                        <dxb:BarItemLinkSeparator/>
                    </dxr:RibbonPageGroup>
                </dxr:RibbonPage>
            </dxr:RibbonControl>

            <!--Detalle de fuente contable-->
            <!--<xctk:BusyIndicator Grid.Row="1" Grid.Column="1" IsBusy="{Binding Path=IsBusy, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">-->
            <Grid x:Name="GridSource" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <!--No está en uso-->
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <!--Titulo o Encabezado-->
                <!--<TextBlock Grid.Row="0" Grid.Column="0" Text="DETALLES DE TERCERO" Grid.ColumnSpan="3" HorizontalAlignment="Center" Padding="10" FontSize="22"/>-->

                <!--Tipo de proceso al que pertenece la fuente contable-->
                <!--adorners:Watermark.Text="Selecciona el tipo de proceso al cual pertenece esta fuente contable"-->

                <TextBlock Grid.Column="0" Grid.Row="1" Text="Régimen" VerticalAlignment="Center" Margin="4" Padding="3"/>
                <dxe:ComboBoxEdit Grid.Column="1" Grid.Row="1" 
                      VerticalAlignment="Center" 
                      Margin="4" Padding="3"
                      IsEnabled="{Binding IsNewRecord, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                      ItemsSource="{Binding RegimeDictionary, UpdateSourceTrigger=PropertyChanged}"
                      DisplayMember="Value"
                      ValueMember="Key"
                      IsTextEditable="False"
                      EditValue="{Binding Path=SelectedRegime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <dxe:ComboBoxEdit.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel/>
                        </ItemsPanelTemplate>
                    </dxe:ComboBoxEdit.ItemsPanel>
                </dxe:ComboBoxEdit>

                <!--Tipo de Documento-->
                <TextBlock Grid.Column="0" Grid.Row="2" 
                       Text="Tipo de Documento" 
                       VerticalAlignment="Center" 
                       Margin="4" Padding="3"/>
                <dxe:ComboBoxEdit Grid.Column="1" Grid.Row="2" 
                     Width="auto" 
                     VerticalAlignment="Center" 
                     Margin="4" Padding="3"
                     IsTextEditable="False"
                     ItemsSource="{Binding IdentificationTypes, UpdateSourceTrigger=PropertyChanged}"
                     DisplayMember="Name"
                     ValueMember="Id"
                     IsEnabled="{Binding IsNewRecord, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" 
                     SelectedItem="{Binding Path=SelectedIdentificationType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!--Numero de documento-->
                <TextBlock Grid.Column="0" Grid.Row="3"
                           Text="Número" 
                           VerticalAlignment="Center" 
                           Margin="4 4 0 2" Padding="3"
                           FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="3" Margin="4 0 4 0">
                    <dxe:SpinEdit x:Name="IdentificationNumber" 
                              Width="auto" MinWidth="60" 
                              Padding="3" Margin="0 2 0 2"
                              MaskType="Numeric"
                              MinValue="0"
                              Mask="n0"
                              AllowDefaultButton="False"
                              dxe:NumericMaskOptions.AlwaysShowDecimalSeparator="False"
                              dxe:NumericMaskOptions.HideInsignificantZeros="True"
                              FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                              MaxLength="15"
                              VerticalContentAlignment="Center"
                              IsReadOnly="{Binding IsNewRecord, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource SimpleBooleanConverter}}"/>
                    <TextBox Width="20" Margin="6 2 0 2"
                             FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                             Text="{Binding Path=VerificationDigit, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                             IsReadOnly="True"
                             IsTabStop="False"
                             VerticalContentAlignment="Center"
                            Visibility="{Binding SelectedIdentificationType.HasVerificationDigit, 
                                         Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={dx:BooleanToVisibilityConverter}}"
                             HorizontalContentAlignment="Center"/>
                </StackPanel>

                <!--Tipo de Captura-->
                <TextBlock Grid.Column="0" Grid.Row="4"
                           Text="Tipo de captura" 
                           VerticalAlignment="Center" 
                           Margin="4 4 0 2" Padding="3"
                           FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                <StackPanel Orientation="Horizontal" 
                            Grid.Column="1" Grid.Row="4" 
                            Width="auto"                       
                            VerticalAlignment="Center" 
                            Margin="4">
                    <RadioButton Content="Persona Natural"
                                 GroupName="CaptureType"
                                 FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize},Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                 IsChecked="{Binding SelectedCaptureType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,
                                 Converter={StaticResource EnumBooleanConverter}, ConverterParameter=PN}"/>
                    <RadioButton Content="Persona Jurídica"
                                 GroupName="CaptureType"
                                 FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                 IsChecked="{Binding SelectedCaptureType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,
                                 Converter={StaticResource EnumBooleanConverter}, ConverterParameter=PJ}"
                                 Margin="5 0 0 0"/>
                </StackPanel>

                <!--Pages de datos de tercero-->
            </Grid>
            <dx:DXTabControl Grid.Column="1" Grid.Row="2" Grid.ColumnSpan="2" 
             Margin="4,4,4,4" Padding="8" 
             TabContentCacheMode="CacheTabsOnSelecting" 
             SelectedIndex="{Binding Path=SelectedIndexPage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <dx:DXTabItem Header="Datos Básicos"
              TextBlock.FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <!--Razon Social-->
                        <TextBlock Text="Razón Social" 
                           Grid.Column="0" Grid.Row="0"
                           Visibility="{Binding CaptureInfoAsPJ, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                           Margin="0 4 4 2"/>
                        <dxe:TextEdit x:Name="BusinessName" 
                            CharacterCasing="Upper"
                            Visibility="{Binding CaptureInfoAsPJ, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="1" 
                            Margin="0 0 0 0" Padding="3"
                            FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <!--Nombres y Apellidos-->
                        <!--Primer Nombre-->
                        <TextBlock Text="Primer Nombre" 
                            Grid.Column="0" Grid.Row="2"
                            Visibility="{Binding CaptureInfoAsPN, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Margin="0 4 4 2"
                            FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxe:TextEdit x:Name="FirstName"
                            CharacterCasing="Upper"
                            Grid.Column="0" Grid.Row="3" 
                            Visibility="{Binding CaptureInfoAsPN, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Margin="0 0 4 0" Padding="3"
                            FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>

                        <!--Segundo Nombre-->
                        <TextBlock Text="Segundo Nombre" 
                            Grid.Column="1" Grid.Row="2"
                            Visibility="{Binding CaptureInfoAsPN, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Margin="4 4 4 2"
                            FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxe:TextEdit x:Name="MiddleName" 
                            CharacterCasing="Upper"
                            Grid.Column="1" Grid.Row="3" 
                            Visibility="{Binding CaptureInfoAsPN, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Margin="4 0 4 0" Padding="3"
                            FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <!--Primer Apellido-->
                        <TextBlock Text="Primer Apellido"
                            Grid.Column="2" Grid.Row="2"
                            Visibility="{Binding CaptureInfoAsPN, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Margin="4 4 4 2"
                            FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxe:TextEdit x:Name="FirstLastName" 
                 CharacterCasing="Upper"
                 Grid.Column="2" Grid.Row="3" 
                 Visibility="{Binding CaptureInfoAsPN, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                 Margin="4 0 4 0" Padding="3"
                 FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <!--Segundo Apellido-->
                        <TextBlock Text="Segundo Apellido"
                   Grid.Column="3" Grid.Row="2"
                   Visibility="{Binding CaptureInfoAsPN, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                   Margin="4 4 0 2"
                   FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxe:TextEdit x:Name="MiddleLastName"
                 CharacterCasing="Upper"
                 Grid.Column="3" Grid.Row="3"
                 Visibility="{Binding CaptureInfoAsPN, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                 Margin="4 0 0 0" Padding="3"
                 FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <!--Nombre Comercial (solo persona natural)-->
                        <TextBlock Text="Nombre Comercial"
                           Grid.Column="0" Grid.Row="4"
                           Visibility="{Binding CaptureInfoAsPN, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                           Margin="0 4 4 2"
                           FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxe:TextEdit x:Name="TradeName"
                            CharacterCasing="Upper"
                            Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="5"
                            Visibility="{Binding CaptureInfoAsPN, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Margin="0 0 0 0" Padding="3"
                            FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <!--Informacion Telefonica-->
                        <!--Telefono fijo 1-->
                        <TextBlock Text="Telefono Fijo No. 1"
                   Grid.Column="0" Grid.Row="6"
                   Margin="0 4 4 2"
                   FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxe:TextEdit x:Name="PrimaryPhone"
                  MaxLength="10"
                  MaskType="Simple"
                  Mask="000 00 00"
                  Grid.Column="0" Grid.Row="7"
                  cal:Message.Attach="[Event LostFocus] = [Action PhoneInputLostFocus($source)]"
                  Margin="0 0 4 0" Padding="3"
                  FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                  Validation.ErrorTemplate="{x:Null}">
                        </dxe:TextEdit>
                        <!--Telefono fijo No 2-->
                        <TextBlock Text="Telefono Fijo No. 2"
                   Grid.Column="1" Grid.Row="6"
                   Margin="4 4 4 2"
                   FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxe:TextEdit x:Name="SecondaryPhone"
                  MaxLength="10"
                  Grid.Column="1" Grid.Row="7"
                  MaskType="Simple"
                  Mask="000 00 00"
                  cal:Message.Attach="[Event LostFocus] = [Action PhoneInputLostFocus($source)]"
                  Margin="4 0 4 0" Padding="3"
                  FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                  Validation.ErrorTemplate="{x:Null}"/>
                        <!--Telefono Celular No 1-->
                        <TextBlock Text="Telefono Celular No. 1"
                   Grid.Column="2" Grid.Row="6"
                   Margin="4 4 4 2"
                   FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxe:TextEdit x:Name="PrimaryCellPhone"
                  MaxLength="14"
                  MaskType="Simple"
                  Mask="000 000 00 00"
                  Grid.Column="2" Grid.Row="7"
                  cal:Message.Attach="[Event LostFocus] = [Action PhoneInputLostFocus($source)]"
                  Margin="4 0 4 0" Padding="3"
                  FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                  Validation.ErrorTemplate="{x:Null}"/>
                        <!--Telefono Celular No 2-->
                        <TextBlock Text="Telefono Celular No. 2"
                   Grid.Column="3" Grid.Row="6"
                   Margin="4 4 0 2"
                   FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxe:TextEdit x:Name="SecondaryCellPhone"
                  MaxLength="14"
                  MaskType="Simple"
                  Mask="000 000 00 00"
                  Grid.Column="3" Grid.Row="7"
                  cal:Message.Attach="[Event LostFocus] = [Action PhoneInputLostFocus($source)]"
                  Margin="4 0 0 0" Padding="3"
                  FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                  Validation.ErrorTemplate="{x:Null}"/>
                        <!--Pais-->
                        <TextBlock Text="Pais"
                   Grid.Column="0" Grid.Row="8"
                   Margin="0 4 0 2"
                   FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxg:LookUpEdit Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="9"
                      Margin="0" Padding="3"
                      AllowSpin="True"
                      AutoPopulateColumns="False"
                      IsCaseSensitiveSearch="False"
                      IncrementalFiltering="True"
                      IncrementalSearch="True"
                      ValidateOnTextInput="False"
                      FilterCondition="Contains"
                      ImmediatePopup="True"
                      ClearSelectionOnBackspace="True"
                      NullText="Seleccione un país..."
                      FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                      ItemsSource="{Binding Path=Countries, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                      DisplayMember="Name"
                      ValueMember="Id"
                      SelectedItem="{Binding Path=SelectedCountry, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <dxg:LookUpEdit.PopupContentTemplate>
                                <ControlTemplate>
                                    <dxg:GridControl Name="PART_GridControl">
                                        <dxg:GridControl.View>
                                            <dxg:TableView AutoWidth="True"/>
                                        </dxg:GridControl.View>
                                        <dxg:GridControl.Columns>
                                            <dxg:GridColumn Header="Nombre del pais" FieldName="Name"/>
                                        </dxg:GridControl.Columns>
                                    </dxg:GridControl>
                                </ControlTemplate>
                            </dxg:LookUpEdit.PopupContentTemplate>
                            <dxg:LookUpEdit.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </dxg:LookUpEdit.ItemsPanel>
                        </dxg:LookUpEdit>
                        <!--Departamento-->
                        <TextBlock Text="Departamento"
                   Grid.Column="0" Grid.Row="10"
                   Margin="0 4 0 2"
                   FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxg:LookUpEdit
                        Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="11"
                        Margin="0" Padding="3"
                        FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                        ItemsSource="{Binding Path=SelectedCountry.Departments, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                        IsCaseSensitiveSearch="False"
                        AutoPopulateColumns="False"
                        IsCaseSensitiveFilter="False"
                        CharacterCasing="Upper"
                        FilterCondition="Contains"
                        ImmediatePopup="True"
                        ClearSelectionOnBackspace="True"
                        NullText="Seleccione un departamento..."
                        DisplayMember="Name"
                        ValueMember="Id"
                        SelectedItem="{Binding SelectedDepartment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <dxg:LookUpEdit.PopupContentTemplate>
                                <ControlTemplate>
                                    <dxg:GridControl Name="PART_GridControl">
                                        <dxg:GridControl.View>
                                            <dxg:TableView AllowCascadeUpdate="True" ShowSearchPanelMode="Never" AutoWidth="True"/>
                                        </dxg:GridControl.View>
                                        <dxg:GridControl.Columns>
                                            <dxg:GridColumn Header="Nombre del departamento" FieldName="Name"/>
                                        </dxg:GridControl.Columns>
                                    </dxg:GridControl>
                                </ControlTemplate>
                            </dxg:LookUpEdit.PopupContentTemplate>
                            <dxg:LookUpEdit.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </dxg:LookUpEdit.ItemsPanel>
                        </dxg:LookUpEdit>
                        <!--Municipio-->
                        <TextBlock Text="Municipio"
                            Grid.Column="0" Grid.Row="12"
                            Margin="0 4 0 2"
                            FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <dxg:LookUpEdit Grid.Column="0" Grid.ColumnSpan="4"
                            Grid.Row="13"
                            Margin="0" Padding="3"
                            ClearSelectionOnBackspace="True"
                            NullText="Seleccione un municipio..."
                            FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                            ItemsSource="{Binding Path=SelectedDepartment.Cities, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                            FilterCondition="Contains"
                            IsCaseSensitiveFilter="False"
                            AutoPopulateColumns="False"
                            CharacterCasing="Upper"
                            ImmediatePopup="True"
                            DisplayMember="Name"
                            ValueMember="Id"
                            EditValue="{Binding SelectedCityId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <dxg:LookUpEdit.PopupContentTemplate>
                                <ControlTemplate>
                                    <dxg:GridControl Name="PART_GridControl">
                                        <dxg:GridControl.View>
                                            <dxg:TableView AllowCascadeUpdate="True" ShowSearchPanelMode="Never" AutoWidth="True"/>
                                        </dxg:GridControl.View>
                                        <dxg:GridControl.Columns>
                                            <dxg:GridColumn Header="Nombre del departamento" FieldName="Name"/>
                                        </dxg:GridControl.Columns>
                                    </dxg:GridControl>
                                </ControlTemplate>
                            </dxg:LookUpEdit.PopupContentTemplate>
                            <dxg:LookUpEdit.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </dxg:LookUpEdit.ItemsPanel>
                        </dxg:LookUpEdit>
                        <!--Direccion-->
                        <TextBlock Text="Direccion"
                   Grid.Column="0" Grid.Row="14"
                   Margin="0 4 0 2"
                   FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxe:TextEdit x:Name="Address"
                     Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="15"
                     Margin="0" Padding="3"
                     FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                </dx:DXTabItem>
                <dx:DXTabItem Header="Retenciones"
              FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                    <ListView ItemsSource="{Binding Path=WithholdingTypes, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Width="auto">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <dxe:CheckEdit IsChecked="{Binding Path=IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="auto" Header="Nombre" DisplayMemberBinding="{Binding Path=Name, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </dx:DXTabItem>

                <dx:DXTabItem Header="Emails"
              FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Grid.Row="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Descripción" 
                       Grid.Column="0" Grid.Row="0" 
                       Margin="0 0 0 2"
                       FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                <TextBlock Text=" ( Ej: Gerencia, Compras )" Foreground="Gray"/>
            </TextBlock>
                        <TextBlock Text="Email" 
                            Grid.Column="1"  Grid.Row="0" 
                            Margin="0 0 0 2"
                            FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                            <TextBlock Text=" ( Ej: micorreo@gmail.com )" Foreground="Gray"/>
                        </TextBlock>
                            <TextBox x:Name="EmailDescription" 
                     Text="{Binding Path=EmailDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     Grid.Column="0" Grid.Row="1" 
                     Width="auto" Margin="0 2 4 2" Padding="4" 
                     FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBox Text="{Binding Path=Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     Grid.Column="1" Grid.Row="1" 
                     Width="auto" Margin="0 2 0 2" Padding="4" 
                     FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Grid.Column="2" Grid.Row="1" Margin="4 2 4 2" Padding="4" Width="auto" x:Name="AddEmail">
                                <Image Source="{dx:DXImage 'Images/Actions/Add_16x16.png'}" Stretch="None">
                                    <Image.Style>
                                        <Style TargetType="Image">
                                            <Style.Triggers>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value=".5"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>
                            </Button>
                        </Grid>

                        <DataGrid Grid.Row="1" Grid.Column="0" Margin="0 4 0 4"
                        AutoGenerateColumns="False"
                        VerticalGridLinesBrush="LightGray"
                        HorizontalGridLinesBrush="LightGray"
                        BorderThickness="0"
                        CanUserAddRows="False"
                        SelectionMode="Single"
                        SelectionUnit="FullRow"     
                        MinRowHeight="30"
                        AlternatingRowBackground="Beige"
                        Background="Transparent"
                        CanUserDeleteRows="False"                                  
                        cal:Message.Attach="[Event CellEditEnding] = [Action EndRowEditing]"
                        ItemsSource="{Binding Path=Emails, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                        SelectedItem="{Binding Path=SelectedEmail, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <DataGrid.Columns>

                                <DataGridTemplateColumn Header="Descripción"
                                    Width="1*">
                                    <DataGridTemplateColumn.HeaderStyle>
                                        <Style TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                        </Style>
                                    </DataGridTemplateColumn.HeaderStyle>
                                    <DataGridTemplateColumn.CellStyle>
                                        <Style TargetType="DataGridCell">
                                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                            <Setter Property="VerticalAlignment" Value="Stretch"/>
                                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                        </Style>
                                    </DataGridTemplateColumn.CellStyle>
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Path=Description, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                       FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                       TextWrapping="Wrap"
                                       VerticalAlignment="Center"
                                       Padding="3"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Path=Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                     VerticalAlignment="Center" 
                                     Padding="3"
                                     FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="Email"
                                    Width="1*">
                                    <DataGridTemplateColumn.HeaderStyle>
                                        <Style TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                        </Style>
                                    </DataGridTemplateColumn.HeaderStyle>
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Path=Email, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                       TextWrapping="Wrap"
                                       VerticalAlignment="Center"
                                       Padding="3"
                                       FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellStyle>
                                        <Style TargetType="DataGridCell">
                                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                            <Setter Property="VerticalAlignment" Value="Stretch"/>
                                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                        </Style>
                                    </DataGridTemplateColumn.CellStyle>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Path=Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     VerticalAlignment="Center"
                                     FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <!--Delete Button-->
                                <DataGridTemplateColumn Width="40">
                                    <DataGridTemplateColumn.CellStyle>
                                        <Style TargetType="DataGridCell">
                                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                            <Setter Property="VerticalAlignment" Value="Stretch"/>
                                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                        </Style>
                                    </DataGridTemplateColumn.CellStyle>
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Margin="4" Padding="4" Command="{Binding Data.DeleteMailCommand, Source={StaticResource DataContextProxy}}">
                                                <Image Source="{dx:DXImage 'Images/Actions/Trash_16x16.png'}" Stretch="None" Margin="3 0 3 0"/>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </dx:DXTabItem>
                <dx:DXTabItem Header="Otros Datos">
                    <Grid Margin="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!--Zona-->
                        <TextBlock Text="Zona"
                                   Grid.Row="0"
                                   Margin="0 4 4 2"
                                   FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <dxg:LookUpEdit Grid.Row="1"
                                        Margin="0 0 0 4" Padding="3"
                                        AllowSpin="True"
                                        AutoPopulateColumns="False"
                                        IsCaseSensitiveSearch="False"
                                        IncrementalFiltering="True"
                                        IncrementalSearch="True"
                                        ValidateOnTextInput="False"
                                        FilterCondition="Contains"
                                        ImmediatePopup="True"
                                        ClearSelectionOnBackspace="True"
                                        NullText="SELECCIONE UNA ZONA..."
                                        FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                        ItemsSource="{Binding Path=Zones, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                        DisplayMember="Name"
                                        ValueMember="Id"
                                        SelectedItem="{Binding Path=SelectedZone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <dxg:LookUpEdit.PopupContentTemplate>
                                <ControlTemplate>
                                    <dxg:GridControl Name="PART_GridControl">
                                        <dxg:GridControl.View>
                                            <dxg:TableView AutoWidth="True"/>
                                        </dxg:GridControl.View>
                                        <dxg:GridControl.Columns>
                                            <dxg:GridColumn Header="Nombre de la zona" FieldName="Name"/>
                                        </dxg:GridControl.Columns>
                                    </dxg:GridControl>
                                </ControlTemplate>
                            </dxg:LookUpEdit.PopupContentTemplate>
                            <dxg:LookUpEdit.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </dxg:LookUpEdit.ItemsPanel>
                        </dxg:LookUpEdit>

                        <!--Plazo a Crédito-->

                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 5 0 5">
                            <TextBlock Text="Plazo a crédito" VerticalAlignment="Center"
                                       Margin="0 0 2 0"
                                       FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                            
                            <dxe:SpinEdit VerticalAlignment="Center" x:Name="CreditTerm"
                                          Text="{Binding CreditTerm, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                          Margin="2 0 2 0"
                                          MaskType="Numeric"
                                          IsFloatValue="False"
                                          MinValue="0" Increment="1"
                                          Mask="n0"
                                          AllowDefaultButton="True"
                                          dxe:NumericMaskOptions.AlwaysShowDecimalSeparator="False"
                                          dxe:NumericMaskOptions.HideInsignificantZeros="True"
                                          FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>

                            <TextBlock Text="(Días)" VerticalAlignment="Center" Margin="2 0 0 0"
                                       FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>

                        </StackPanel>


                        <!--Exento de Impuestos-->

                        <dxe:CheckEdit  Grid.Row="3" Margin="0 5 0 5"
                                        IsChecked="{Binding IsTaxFree, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                        Content="¿Este cliente está excento de impuestos?"
                                        Style="{StaticResource BlueCheckedTextStyle}"
                                        FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>

                        <!--Retiene sobre cualquier base-->

                        <dxe:CheckEdit  Grid.Row="4" Margin="0 5 0 5"
                                        IsChecked="{Binding RetainsOnAnyBasis, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                        Content="¿Este cliente retiene sobre cualquier base?"
                                        Style="{StaticResource BlueCheckedTextStyle}"
                                        FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>

                        <!--Estado Activo-->
                        <dxe:CheckEdit  Grid.Row="5" Margin="0 5 0 5"
                                        IsChecked="{Binding IsActive, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Converter={dxmvvm:BooleanNegationConverter}}"
                                        Content="Bloquear las ventas para este cliente"
                                        Style="{StaticResource BlueCheckedTextStyle}"
                                        FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>

                        <!--Motivo de Bloqueo - Solo visible cuando el cliente está bloqueado (IsActive = false)-->
                        <TextBlock Text="Motivo de Bloqueo"
                                   Grid.Row="6"
                                   Margin="0 4 0 2"
                                   FontSize="{Binding Source={x:Static config:UICustom.LabelFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                   Visibility="{Binding Path=IsActive, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={dx:BooleanToVisibilityConverter Invert=True}}"/>

                        <dxe:TextEdit x:Name="BlockingReason"
                                      Grid.Row="7"
                                      Margin="0 0 0 4" Padding="3"
                                      CharacterCasing="Upper"
                                      FontSize="{Binding Source={x:Static config:UICustom.ControlFontSize}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                      Visibility="{Binding Path=IsActive, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={dx:BooleanToVisibilityConverter Invert=True}}"/>
                    </Grid>
                </dx:DXTabItem>
            </dx:DXTabControl>

        </Grid>

    </busyIndicator:BusyMask>
</UserControl>
