<UserControl x:Class="NetErp.Global.Collaborator.Views.CollaboratorInviteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:h="clr-namespace:NetErp.Helpers"
             xmlns:cal="http://caliburnmicro.com"
             xmlns:busyIndicator="https://github.com/moh3ngolshani/BusyIndicator"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="500"
             Background="#EEF1F5">

    <busyIndicator:BusyMask x:Name="BusyIndicator"
                            IsBusy="{Binding IsBusy}"
                            IndicatorType="BouncingDot"
                            BusyContent="Por favor espere..."
                            BusyContentMargin="0,20,0,0"
                            IsBusyAtStartup="False">

        <Border Background="White" BorderBrush="#D9DDE3" BorderThickness="1"
                CornerRadius="8" Margin="8,0,8,8"
                Width="{Binding DialogWidth}"
                Height="{Binding DialogHeight}">

            <Grid Margin="15,15,15,5" h:EnterKeyTraversal.IsEnabled="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0: Form Content -->
                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                    <StackPanel>

                        <!-- Búsqueda por email -->
                        <TextBlock Text="Correo electrónico del colaborador" FontSize="13" Margin="0,0,0,3"/>
                        <dxe:TextEdit x:Name="SearchEmailBox"
                                      Text="{Binding SearchEmail, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Delay=500}"
                                      Style="{StaticResource FocusedHighlightStyle}"
                                      NullText="Escribe el correo electrónico para buscar..."
                                      Padding="3"
                                      BorderBrush="#C8CED6"
                                      Margin="0,0,0,15"/>

                        <!-- Resultados de búsqueda -->
                        <ItemsControl ItemsSource="{Binding SearchResults}"
                                      Visibility="{Binding AccountFound, Converter={dxmvvm:BooleanToVisibilityConverter}}"
                                      Margin="0,0,0,15" Focusable="False">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="1"
                                            CornerRadius="8" Padding="12" Margin="0,0,0,4" Cursor="Hand"
                                            cal:Message.Attach="[Event MouseLeftButtonDown] = [Action SelectAccount($dataContext)]">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#f8f9fa"/>
                                                <Setter Property="BorderBrush" Value="#dee2e6"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="BorderBrush" Value="#007bff"/>
                                                    </Trigger>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="BorderBrush" Value="#007bff"/>
                                                        <Setter Property="Background" Value="#e3f2fd"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Avatar -->
                                            <Border Grid.Column="0" Width="40" Height="40"
                                                    CornerRadius="20" Background="#007bff"
                                                    Margin="0,0,12,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding FirstName[0]}"
                                                           FontSize="16" FontWeight="Bold" Foreground="White"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>

                                            <!-- Info -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding FullName}"
                                                           FontSize="14" FontWeight="SemiBold" Foreground="#2c3e50"/>
                                                <TextBlock Text="{Binding Email}"
                                                           FontSize="12" Foreground="#6c757d" Margin="0,1,0,0"/>
                                                <TextBlock Text="{Binding Profession}"
                                                           FontSize="11" Foreground="#868e96" FontStyle="Italic" Margin="0,1,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- No encontrado -->
                        <Border Visibility="{Binding AccountNotFound, Converter={dxmvvm:BooleanToVisibilityConverter}}"
                                Background="#fff3cd" BorderBrush="#ffc107" BorderThickness="1"
                                CornerRadius="6" Padding="12" Margin="0,0,0,15">
                            <TextBlock Text="No se encontró una cuenta con ese correo electrónico"
                                       FontSize="13" Foreground="#856404" TextWrapping="Wrap"/>
                        </Border>


                    </StackPanel>
                </ScrollViewer>

                <!-- Row 1: Action Buttons -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,10">

                    <!-- Invitar -->
                    <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                            IsEnabled="{Binding CanInvite}"
                            Command="{Binding InviteCommand}">
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Save/Save_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.4"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="Invitar" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Button>

                    <!-- Cancelar -->
                    <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                            IsCancel="True"
                            Command="{Binding CancelCommand}">
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Actions/Cancel_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.4"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="Cancelar" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Button>

                </StackPanel>

            </Grid>
        </Border>
    </busyIndicator:BusyMask>
</UserControl>
