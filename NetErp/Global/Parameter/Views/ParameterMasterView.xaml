<UserControl x:Class="NetErp.Global.Parameter.Views.ParameterMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetErp.Global.Parameter.Views"
             mc:Ignorable="d" 
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:config="clr-namespace:Common.Config;assembly=Common"
             xmlns:h="clr-namespace:NetErp.Helpers"
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:cal="http://caliburnmicro.com"
             xmlns:busyIndicator="https://github.com/moh3ngolshani/BusyIndicator"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             >
    <UserControl.Resources>
        <h:EnumBooleanConverter x:Key="EnumBooleanConverter" />
        <h:ObjectEqualityMultiConverter  x:Key="ObjectEqualityMultiConverter" />
        <h:BindingProxy x:Key="DataContextProxy" Data="{Binding}"/>
    </UserControl.Resources>
    <busyIndicator:BusyMask x:Name="BusyIndicator"
     IsBusy="{Binding IsBusy}"
     IndicatorType="BouncingDot"
     BusyContent="Please wait..."
     BusyContentMargin="0,20,0,0"
     IsBusyAtStartup="False">
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <dx:DXTabControl Background="Transparent"
             TabContentCacheMode="CacheTabsOnSelecting"
             HorizontalContentAlignment="Stretch"
             Margin="20 0 0 0"
             SelectedIndex="{Binding Data.SelectedIndex, UpdateSourceTrigger=PropertyChanged, Source={StaticResource DataContextProxy}}">
                <dx:DXTabItem Header="Contabilidad">

                    <ContentControl   cal:View.Model="{Binding DynamicControlBook}" />
                </dx:DXTabItem>
                <dx:DXTabItem Header="Inventarios">
                    <ContentControl   cal:View.Model="{Binding DynamicControlInventory}" />
                </dx:DXTabItem>
                <dx:DXTabItem Header="Tesorería">
                    <ContentControl   cal:View.Model="{Binding DynamicControTreasury}" />
                </dx:DXTabItem>
                <dx:DXTabItem Header="Ventas">
                    <ContentControl   cal:View.Model="{Binding DynamicControlBilling}" />
                </dx:DXTabItem>
                <dx:DXTabItem Header="Generales">
                    <ContentControl   cal:View.Model="{Binding DynamicControlGlobal}" />
                </dx:DXTabItem>
            </dx:DXTabControl>
       
        
        <StackPanel Grid.Row="1" >

            <Button
         BorderThickness="0"
         Padding="3"
         Margin="3"
         Width="auto" Height="auto" 
         cal:Message.Attach="[Event Click] = [Action save]">
                <Button.Content>
                    <Image Source="{dx:SvgImageSource Uri='pack://application:,,,/DevExpress.Images.v25.2;component/SvgImages/XAF/Action_Edit.svg', Size='60,70'}" Stretch="None" />
                </Button.Content>
            </Button>

        </StackPanel>
    </Grid>
    </busyIndicator:BusyMask>
</UserControl>
