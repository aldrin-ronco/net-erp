<UserControl x:Class="NetErp.Inventory.ItemSizes.Views.ItemSizeCategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:h="clr-namespace:NetErp.Helpers"
             xmlns:viewmodels="clr-namespace:NetErp.Inventory.ItemSizes.ViewModels"
             xmlns:model="clr-namespace:NetErp.Inventory.ItemSizes.DTO"
             d:DataContext="{d:DesignInstance Type=viewmodels:ItemSizeCategoryViewModel}"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:busyIndicator="https://github.com/moh3ngolshani/BusyIndicator"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <h:BindingProxy x:Key="DataContextProxy" Data="{Binding}"/>

        <!-- Category template -->
        <HierarchicalDataTemplate DataType="{x:Type model:ItemSizeCategoryDTO}" ItemsSource="{Binding ItemSizeValues}">
            <StackPanel Orientation="Horizontal" Margin="-1,0,0,0" VerticalAlignment="Center" Background="Transparent">
                <StackPanel.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Crear grupo de tallaje" Command="{Binding Data.CreateCategoryCommand, Source={StaticResource DataContextProxy}}">
                            <MenuItem.Icon>
                                <Image Source="{dx:DXImage 'Images/Actions/AddItem_16x16.png'}"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="Crear tallaje" Command="{Binding Data.CreateValueCommand, Source={StaticResource DataContextProxy}}">
                            <MenuItem.Icon>
                                <Image Source="{dx:DXImage 'Images/Actions/AddItem_16x16.png'}"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Command="{Binding Data.DeleteItemSizeCategoryCommand, Source={StaticResource DataContextProxy}}">
                            <MenuItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Eliminar grupo de tallaje: "/>
                                    <TextBlock Text="{Binding Row.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                            </MenuItem.Header>
                            <MenuItem.Icon>
                                <Image Source="{dx:DXImage 'Images/Edit/Delete_16x16.png'}"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </StackPanel.ContextMenu>
                <Image Width="18" Height="18" VerticalAlignment="Center">
                    <Image.Style>
                        <Style TargetType="Image">
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Row.IsExpanded}" Value="false"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Source" Value="{dx:DXImage 'SvgImages/Icon Builder/Actions_FolderClose.svg'}"/>
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Row.IsExpanded}" Value="true"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Source" Value="{dx:DXImage 'SvgImages/Icon Builder/Actions_FolderOpen.svg'}"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
                <TextBlock Text="{Binding Row.Name, UpdateSourceTrigger=PropertyChanged}" Margin="2,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </HierarchicalDataTemplate>

        <!-- Value template -->
        <DataTemplate DataType="{x:Type model:ItemSizeValueDTO}">
            <StackPanel Orientation="Horizontal" Margin="-1,0,0,0" VerticalAlignment="Center" Background="Transparent">
                <StackPanel.ContextMenu>
                    <ContextMenu>
                        <MenuItem Command="{Binding Data.DeleteItemSizeValueCommand, Source={StaticResource DataContextProxy}}">
                            <MenuItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Eliminar tallaje: "/>
                                    <TextBlock Text="{Binding Row.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                            </MenuItem.Header>
                            <MenuItem.Icon>
                                <Image Source="{dx:DXImage 'Images/Edit/Delete_16x16.png'}"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </StackPanel.ContextMenu>
                <Image Width="18" Height="18" Source="{dx:DXImage 'Images/Miscellaneous/HighlightActiveElements_16x16.png'}" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Row.Name, UpdateSourceTrigger=PropertyChanged}" Margin="2,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <busyIndicator:BusyMask x:Name="BusyIndicator"
                            IsBusy="{Binding IsBusy}"
                            IndicatorType="BouncingDot"
                            BusyContent="Please wait..."
                            BusyContentMargin="0,20,0,0">

        <Border Background="White" BorderBrush="#D9DDE3" BorderThickness="1"
                CornerRadius="8" Margin="10" Padding="10,10,10,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.4*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Column: DevExpress TreeViewControl -->
                <dxg:TreeViewControl Grid.Column="0" Margin="0,10,0,0"
                                     ItemsSource="{Binding ItemSizeCategories, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     h:TreeViewExtension.SelectItemOnRightClick="True"
                                     HighlightSelectedNodeIndent="False"
                                     BorderThickness="0"
                                     ShowExpandButtons="True"
                                     ShowBorder="False"
                                     AllowDragDrop="False"
                                     AllowEditing="False"
                                     ExpandStateBinding="{Binding IsExpanded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     SelectedItem="{Binding SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     TreeDerivationMode="HierarchicalDataTemplate"
                                     IsEnabled="{Binding TreeViewIsEnable, UpdateSourceTrigger=PropertyChanged}">
                    <dxg:TreeViewControl.NodeStyle>
                        <Style TargetType="dxg:RowControl">
                            <Setter Property="Foreground" Value="Black"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Row.IsExpanded}" Value="true"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Foreground" Value="Blue"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </dxg:TreeViewControl.NodeStyle>
                </dxg:TreeViewControl>

                <!-- Right Column: Detail Panel -->
                <Grid Grid.Column="1" Margin="10,0,0,0"
                      Visibility="{Binding DetailPanelVisibility, Converter={dx:BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Detail form with Ubicación inside -->
                    <Border Grid.Row="0" BorderBrush="#D9DDE3" BorderThickness="1" CornerRadius="6"
                            VerticalAlignment="Top" HorizontalAlignment="Stretch"
                            Margin="0,10,0,0" Padding="0">
                        <StackPanel>
                            <!-- Ubicación header -->
                            <Border Background="#F5F5F5" Padding="12,8"
                                    CornerRadius="6,6,0,0">
                                <StackPanel>
                                    <TextBlock Text="Ubicación" Foreground="#909090" FontSize="11" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Width="14" Height="14" VerticalAlignment="Center"
                                               Source="{dx:DXImage 'SvgImages/Icon Builder/Actions_FolderClose.svg'}"/>
                                        <TextBlock Text="{Binding BreadcrumbCategoryName}" Margin="4,0,0,0"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text="  /  " VerticalAlignment="Center" Foreground="#909090"
                                                   Visibility="{Binding BreadcrumbHasValue, Converter={dx:BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}"/>
                                        <Image Width="14" Height="14" VerticalAlignment="Center"
                                               Source="{dx:DXImage 'Images/Miscellaneous/HighlightActiveElements_16x16.png'}"
                                               Visibility="{Binding BreadcrumbHasValue, Converter={dx:BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}"/>
                                        <TextBlock Text="{Binding BreadcrumbValueName}" Margin="4,0,0,0"
                                                   VerticalAlignment="Center"
                                                   Visibility="{Binding BreadcrumbHasValue, Converter={dx:BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Form fields -->
                            <StackPanel Margin="15,15,15,10"
                                        IsEnabled="{Binding IsEditing, UpdateSourceTrigger=PropertyChanged}">
                                <TextBlock Text="Nombre" Margin="0,0,0,4"/>
                                <dxe:TextEdit x:Name="EditingName" Margin="0,0,0,10"
                                              EditValue="{Binding EditingName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              CharacterCasing="Upper" Padding="3" BorderBrush="#C8CED6"/>

                                <TextBlock Text="Orden de presentación" Margin="0,0,0,4"
                                           Visibility="{Binding IsValueSelected, Converter={dx:BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}"/>
                                <dxe:SpinEdit Margin="0,0,0,5" MaxWidth="80" HorizontalAlignment="Left"
                                              EditValue="{Binding EditingDisplayOrder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              EditValueType="{x:Type sys:Int32}"
                                              IsFloatValue="False" MinValue="0" Increment="1" Padding="3" BorderBrush="#C8CED6"
                                              Visibility="{Binding IsValueSelected, Converter={dx:BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Action buttons -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,20">
                        <!-- Editar -->
                        <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                                Command="{Binding EditCommand}"
                                IsEnabled="{Binding CanEdit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <StackPanel HorizontalAlignment="Center">
                                <Image Source="{dx:DXImage 'Images/Edit/Edit_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                    <Image.Style>
                                        <Style TargetType="Image">
                                            <Style.Triggers>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.4"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>
                                <TextBlock Text="Editar" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Button>
                        <!-- Deshacer -->
                        <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                                Command="{Binding UndoCommand}"
                                IsEnabled="{Binding CanUndo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <StackPanel HorizontalAlignment="Center">
                                <Image Source="{dx:DXImage 'Images/History/Undo_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                    <Image.Style>
                                        <Style TargetType="Image">
                                            <Style.Triggers>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.4"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>
                                <TextBlock Text="Deshacer" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Button>
                        <!-- Guardar -->
                        <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                                Command="{Binding SaveCommand}"
                                IsEnabled="{Binding CanSave, UpdateSourceTrigger=PropertyChanged}">
                            <StackPanel HorizontalAlignment="Center">
                                <Image Source="{dx:DXImage 'Images/Save/Save_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                    <Image.Style>
                                        <Style TargetType="Image">
                                            <Style.Triggers>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.4"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>
                                <TextBlock Text="Guardar" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </busyIndicator:BusyMask>
</UserControl>
