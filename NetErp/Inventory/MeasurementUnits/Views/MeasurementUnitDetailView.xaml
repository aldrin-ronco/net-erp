<UserControl x:Class="NetErp.Inventory.MeasurementUnits.Views.MeasurementUnitDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:busyIndicator="https://github.com/moh3ngolshani/BusyIndicator"
             xmlns:h="clr-namespace:NetErp.Helpers"
             xmlns:local="clr-namespace:NetErp.Inventory.MeasurementUnits.Views"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="500"
             Background="#EEF1F5">
    <busyIndicator:BusyMask x:Name="BusyIndicator"
                            IsBusy="{Binding IsBusy}"
                            IndicatorType="BouncingDot"
                            BusyContent="Please wait..."
                            BusyContentMargin="0,20,0,0">
        <Border Background="White" BorderBrush="#D9DDE3" BorderThickness="1"
                CornerRadius="8" Margin="20" MinWidth="420">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Form content -->
                <StackPanel Grid.Row="0" Margin="30,25,30,20">

                    <!-- Title -->
                    <TextBlock Text="Información de Unidad de Medida"
                               FontSize="16" FontWeight="Bold"
                               Margin="0,0,0,20"/>

                    <!-- Nombre -->
                    <TextBlock Text="Nombre" FontSize="13" Margin="0,0,0,5"/>
                    <dxe:TextEdit Text="{Binding Path=Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                  CharacterCasing="Upper"
                                  x:Name="MeasurementUnitName"
                                  h:FocusBehavior.IsFocused="True"
                                  BorderBrush="#C8CED6"
                                  Margin="0,0,0,15"/>

                    <!-- Abreviación -->
                    <TextBlock Text="Abreviación" FontSize="13" Margin="0,0,0,5"/>
                    <dxe:TextEdit Text="{Binding Path=Abbreviation, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                  CharacterCasing="Upper"
                                  BorderBrush="#C8CED6"
                                  Margin="0,0,0,15"/>

                    <!-- Tipo -->
                    <TextBlock Text="Tipo" FontSize="13" Margin="0,0,0,5"/>
                    <dxe:ComboBoxEdit
                        ItemsSource="{Binding MeasurementUnitTypes}"
                        EditValue="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        DisplayMember="Value"
                        ValueMember="Key"
                        IsTextEditable="False"
                        BorderBrush="#C8CED6"
                        Margin="0,0,0,15"/>

                    <!-- Código DIAN -->
                    <TextBlock Text="Código DIAN" FontSize="13" Margin="0,0,0,5"/>
                    <dxe:TextEdit Text="{Binding Path=DianCode, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                  BorderBrush="#C8CED6"
                                  Margin="0,0,0,0"/>
                </StackPanel>

                <!-- Button bar centered -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,20">
                    <!-- Guardar -->
                    <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                            IsEnabled="{Binding CanSave, UpdateSourceTrigger=PropertyChanged}">
                        <dxmvvm:Interaction.Behaviors>
                            <dxmvvm:EventToCommand Command="{Binding SaveCommand}" EventName="Click"/>
                        </dxmvvm:Interaction.Behaviors>
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Save/Save_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.4"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="Guardar" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Button>
                    <!-- Cancelar -->
                    <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                            IsCancel="True">
                        <dxmvvm:Interaction.Behaviors>
                            <dxmvvm:EventToCommand Command="{Binding CancelCommand}" EventName="Click"/>
                        </dxmvvm:Interaction.Behaviors>
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Actions/Cancel_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center"/>
                            <TextBlock Text="Cancelar" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </busyIndicator:BusyMask>
</UserControl>
