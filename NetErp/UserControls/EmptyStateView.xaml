<UserControl x:Class="NetErp.UserControls.EmptyStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="500">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Image Source="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
               Width="{Binding ImageWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
               HorizontalAlignment="Center" Margin="0,0,0,14"/>
        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="16" Foreground="#888" HorizontalAlignment="Center" Margin="0,0,0,6"/>
        <TextBlock Text="{Binding Message, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="12" Foreground="#AAA" HorizontalAlignment="Center" Margin="0,0,0,14"/>
        <TextBlock Text="{Binding ContextInfo, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="11" Foreground="#999" FontStyle="Italic"
                   HorizontalAlignment="Center" TextAlignment="Center"
                   TextWrapping="Wrap" MaxWidth="500" Margin="0,0,0,16">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ContextInfo, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <Button x:Name="ActionButton"
                Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                Click="OnActionButtonClick"
                Visibility="{Binding ButtonVisible, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={dxmvvm:BooleanToVisibilityConverter}}">
            <Grid>
                <StackPanel HorizontalAlignment="Center">
                    <Image Source="{Binding ButtonIcon, RelativeSource={RelativeSource AncestorType=UserControl}, TargetNullValue={dx:DXImage 'Images/Actions/New_32x32.png'}}"
                           Width="32" Height="32" HorizontalAlignment="Center">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.7"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>
                    <TextBlock Text="{Binding ButtonText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
                <Image Source="{dx:DXImage 'Images/XAF/BO_Security_Permission.png'}"
                       Width="14" Height="14"
                       HorizontalAlignment="Right" VerticalAlignment="Bottom"
                       Margin="0,0,-2,12">
                    <Image.Style>
                        <Style TargetType="Image">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasPermission, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
            </Grid>
        </Button>
    </StackPanel>
</UserControl>
