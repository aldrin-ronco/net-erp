<UserControl x:Class="NetErp.Books.IdentificationTypes.Views.IdentificationTypeDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:busyIndicator="https://github.com/moh3ngolshani/BusyIndicator"
             xmlns:h="clr-namespace:NetErp.Helpers"
             mc:Ignorable="d"
             Background="#EEF1F5"
             d:DesignHeight="300" d:DesignWidth="450">

    <busyIndicator:BusyMask x:Name="BusyIndicator"
                            IsBusy="{Binding IsBusy}"
                            IndicatorType="BouncingDot"
                            BusyContent="Por favor espere..."
                            BusyContentMargin="0,20,0,0">
        <Border Background="White" BorderBrush="#D9DDE3" BorderThickness="1"
                CornerRadius="8" Margin="8,0,8,8" MinWidth="400">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Form content -->
                <StackPanel Grid.Row="0" Margin="30,25,30,20">

                    <!-- Código -->
                    <TextBlock Text="Código DIAN (2 dígitos)" FontSize="13" Margin="0,0,0,5"/>
                    <dxe:TextEdit x:Name="IdentificationTypeCode"
                                  Style="{StaticResource FocusedHighlightStyle}"
                                  Text="{Binding Path=Code, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                  MaxLength="2"
                                  IsReadOnly="{Binding IsReadOnlyCode}"
                                  BorderBrush="#C8CED6"
                                  MaskType="RegEx"
                                  Mask="[0-9]{1,2}"
                                  h:FocusBehavior.IsFocused="True"
                                  Margin="0,0,0,15"/>

                    <!-- Nombre -->
                    <TextBlock Text="Nombre" FontSize="13" Margin="0,0,0,5"/>
                    <dxe:TextEdit x:Name="IdentificationTypeName"
                                  Style="{StaticResource FocusedHighlightStyle}"
                                  Text="{Binding Path=Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                  CharacterCasing="Upper"
                                  BorderBrush="#C8CED6"
                                  Margin="0,0,0,15"/>

                    <!-- Longitud mínima -->
                    <TextBlock Text="Longitud mínima del documento" FontSize="13" Margin="0,0,0,5"/>
                    <dxe:SpinEdit Value="{Binding Path=MinimumDocumentLength, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                  MinValue="5" MaxValue="15"
                                  IsFloatValue="False"
                                  BorderBrush="#C8CED6"
                                  HorizontalAlignment="Left"
                                  MinWidth="80"
                                  Margin="0,0,0,15"/>

                    <!-- Permite letras -->
                    <dxe:CheckEdit Content="Permite letras en el documento"
                                   Style="{StaticResource BlueCheckedTextStyle}" Padding="2"
                                   IsChecked="{Binding Path=AllowsLetters, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                   Margin="0,0,0,8"/>

                    <!-- Dígito de verificación -->
                    <dxe:CheckEdit Content="Requiere dígito de verificación"
                                   Style="{StaticResource BlueCheckedTextStyle}" Padding="2"
                                   IsChecked="{Binding Path=HasVerificationDigit, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
                </StackPanel>

                <!-- Button bar centered -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,20">
                    <!-- Guardar -->
                    <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                            IsEnabled="{Binding CanSave, UpdateSourceTrigger=PropertyChanged}">
                        <dxmvvm:Interaction.Behaviors>
                            <dxmvvm:EventToCommand Command="{Binding SaveCommand}" EventName="Click"/>
                        </dxmvvm:Interaction.Behaviors>
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Save/Save_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.4"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="Guardar" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Button>
                    <!-- Cancelar -->
                    <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                            IsCancel="True">
                        <dxmvvm:Interaction.Behaviors>
                            <dxmvvm:EventToCommand Command="{Binding CancelCommand}" EventName="Click"/>
                        </dxmvvm:Interaction.Behaviors>
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Actions/Cancel_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center"/>
                            <TextBlock Text="Cancelar" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </busyIndicator:BusyMask>
</UserControl>
