<UserControl x:Class="NetErp.Books.AccountingSources.Views.AccountingSourceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:busyIndicator="https://github.com/moh3ngolshani/BusyIndicator"
             xmlns:uc="clr-namespace:NetErp.UserControls"
             xmlns:h="clr-namespace:NetErp.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Background="White" BorderBrush="#D9DDE3" BorderThickness="1"
            CornerRadius="8" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Row 0: Module filter -->
            <StackPanel Grid.Row="0" Margin="10,10,10,0">
                <TextBlock Text="Módulo administrativo" Margin="0,0,0,3"/>
                <dxe:ComboBoxEdit
                    ItemsSource="{Binding Modules, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                    DisplayMember="Name"
                    ValueMember="Id"
                    IsTextEditable="False"
                    NullText="Mostrar todos los módulos"
                    NullValueButtonPlacement="EditBox"
                    EditValue="{Binding SelectedModuleId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    BorderBrush="#C8CED6"/>
            </StackPanel>

            <!-- Row 1: Search label -->
            <TextBlock Grid.Row="1" Text="¿Qué desea buscar?" Margin="10,10,10,3"/>

            <!-- Row 2: Search box -->
            <dxe:ButtonEdit
                Grid.Row="2"
                Margin="10,0,10,0"
                NullText="Escribe por lo menos 3 letras para iniciar la busqueda"
                ToolTip="Escribe aqui para buscar"
                MinHeight="25"
                CharacterCasing="Upper"
                BorderBrush="#C8CED6"
                AllowDefaultButton="False"
                h:FocusBehavior.IsFocused="True"
                Text="{Binding FilterSearch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Delay=500}">
                <dxe:ButtonEdit.Buttons>
                    <dxe:ButtonInfo IsLeft="True" GlyphKind="Search" Margin="4,0,2,0"/>
                </dxe:ButtonEdit.Buttons>
            </dxe:ButtonEdit>

            <!-- Row 3: Grid -->
            <busyIndicator:BusyMask x:Name="BusyIndicator"
                                    IsBusy="{Binding IsBusy}"
                                    IndicatorType="BouncingDot"
                                    BusyContent="Por favor espere..."
                                    BusyContentMargin="0,20,0,0"
                                    Grid.Row="3">
                <dxg:GridControl Margin="10,6,10,0"
                                 EnableSmartColumnsGeneration="True"
                                 AutoGenerateColumns="None"
                                 SelectedItem="{Binding Path=SelectedAccountingSource, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 ItemsSource="{Binding Path=AccountingSources, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <dxg:GridControl.View>
                        <dxg:TableView AllowEditing="False"
                                       AllowCascadeUpdate="False"
                                       EnableSelectedRowAppearance="True"
                                       ShowGroupPanel="False"
                                       AllowColumnMoving="False"
                                       IsColumnMenuEnabled="False"
                                       ColumnFilterPopupMode="Disabled"
                                       AllowSorting="False"
                                       AllowColumnFiltering="False"
                                       AlternateRowBackground="Beige"/>
                    </dxg:GridControl.View>

                    <dxg:GridColumn Header="Código" Width="Auto" HorizontalHeaderContentAlignment="Center"
                                    Binding="{Binding Code}"/>
                    <dxg:GridColumn Header="Nombre" Width="*" HorizontalHeaderContentAlignment="Center"
                                    Binding="{Binding Name}"/>
                    <dxg:GridColumn Header="Tipo de proceso" Width="Auto" MinWidth="150" HorizontalHeaderContentAlignment="Center"
                                    Binding="{Binding ProcessType.Name}"/>
                    <dxg:GridColumn Header="Módulo" Width="Auto" MinWidth="120" HorizontalHeaderContentAlignment="Center"
                                    Binding="{Binding ProcessType.MenuModule.Name}"/>
                    <dxg:GridColumn Header="Sistema" Width="Auto" MinWidth="70" HorizontalHeaderContentAlignment="Center"
                                    Binding="{Binding IsSystemSource, Converter={dxmvvm:BooleanToObjectConverter TrueValue='Sí', FalseValue='No'}}"/>
                </dxg:GridControl>
            </busyIndicator:BusyMask>

            <!-- Row 4: Pagination -->
            <uc:GridPaging Grid.Row="4"
                           Margin="10,6,10,0"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Center"
                           ChangedIndexCommand="{Binding PaginationCommand}"
                           PageIndex="{Binding PageIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                           PageSize="{Binding PageSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                           TotalCount="{Binding TotalCount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                           ResponseTime="{Binding ResponseTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <!-- Row 5: Action buttons centered -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,10">
                <!-- Nuevo -->
                <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                        IsEnabled="{Binding CanCreateSource}"
                        Command="{Binding CreateSourceCommand}">
                    <Grid>
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Actions/New_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.7"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="Nuevo" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                        <Image Source="{dx:DXImage 'Images/XAF/BO_Security_Permission.png'}" Width="14" Height="14"
                               HorizontalAlignment="Right" VerticalAlignment="Bottom"
                               Margin="0,0,-2,12">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasCreatePermission}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                    </Grid>
                </Button>
                <!-- Editar -->
                <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                        IsEnabled="{Binding CanEditSource}"
                        Command="{Binding EditSourceCommand}">
                    <Grid>
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Edit/Edit_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.7"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="Editar" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                        <Image Source="{dx:DXImage 'Images/XAF/BO_Security_Permission.png'}" Width="14" Height="14"
                               HorizontalAlignment="Right" VerticalAlignment="Bottom"
                               Margin="0,0,-2,12">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasEditPermission}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                    </Grid>
                </Button>
                <!-- Eliminar -->
                <Button Background="Transparent" BorderThickness="0" Padding="15,5" Cursor="Hand"
                        IsEnabled="{Binding CanDeleteSource}"
                        Command="{Binding DeleteSourceCommand}">
                    <Grid>
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{dx:DXImage 'Images/Actions/DeleteList_32x32.png'}" Width="32" Height="32" HorizontalAlignment="Center">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.7"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="Eliminar" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                        <Image Source="{dx:DXImage 'Images/XAF/BO_Security_Permission.png'}" Width="14" Height="14"
                               HorizontalAlignment="Right" VerticalAlignment="Bottom"
                               Margin="0,0,-2,12">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasDeletePermission}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                    </Grid>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
